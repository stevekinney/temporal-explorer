# Workstreams

## Active Workstreams

| Workstream                       | Owner | Status   | Editable Scope                                                                                                                                    | Verification                                                                                                                                            | Synchronization                                                                              |
| -------------------------------- | ----- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| Stage 0 skeleton                 | main  | complete | Root package metadata, `apps/explorer`, `packages/api`, `packages/cli`, `packages/schemas`, `fixtures/basic-order`, implementation tracking files | `bun install`, `bun run typecheck`, `bun test`, `bunx temporal-explorer --help`, `bun run validate`                                                     | Package boundaries established before schema and analyzer work.                              |
| Stage 1 schema and fixture truth | main  | complete | `packages/schemas`, fixture generation and validation scripts, `fixtures/basic-order` artifacts                                                   | `bun test packages/schemas`, `bun run fixtures:generate-histories`, `bun run fixtures:validate`, `bun run fixtures:verify-no-drift`, `bun run validate` | Schema contracts and real history fixture established.                                       |
| Stage 2 static analyzer slice    | main  | complete | `packages/analyzer`, `packages/api`, `packages/cli`, `fixtures/basic-order/.temporal-explorer`                                                    | `bun test packages/analyzer`, `bun test packages/api`, analyzer CLI commands, schema validation, `bun run test:cli -- --fixture basic-order`            | CLI calls public API; analyzer owns source inspection.                                       |
| Stage 3 runtime trace slice      | main  | complete | `packages/history`, `packages/api`, `packages/cli`, `fixtures/basic-order/.temporal-explorer/histories`, fixture gates                            | `bun test packages/history`, history import CLI command, trace schema validation, `bun run test:fixtures -- --fixture basic-order-history`, validation  | Trace parsing is independent from static analysis.                                           |
| Stage 4 source-aware overlay     | main  | complete | `packages/mapper`, `packages/api`, `packages/cli`, overlay schema, `fixtures/basic-order/.temporal-explorer/overlays`                             | `bun test packages/mapper`, trace CLI command, overlay schema validation, report CLI command, `bun run test:cli -- --fixture basic-order-overlay`       | Mapping evidence is explicit; replay remains optional later.                                 |
| Stage 5 documentation renderers  | main  | complete | `packages/renderers`, `packages/api`, `packages/cli`, `scripts/snapshots`, `fixtures/basic-order/.temporal-explorer/docs`                         | `bun test packages/renderers`, docs CLI command, Mermaid render CLI command, `bun run snapshots:verify`, validation                                     | Generated docs stay deterministic and Prettier-compatible.                                   |
| Stage 6 local explorer shell     | main  | complete | `apps/explorer`, `packages/cli/src/open-server.ts`, `scripts/ui`, SvelteKit/Cinder configuration                                                  | `bun test apps/explorer`, open CLI command, `bun run ui:e2e`, `bun run ui:accessibility`, `bun run ui:server-lifecycle`                                 | UI consumes validated artifacts only; Cinder issue filed upstream as stevekinney/cinder#580. |
| Stage 7 graph and trace UI       | main  | complete | `apps/explorer`, `packages/cli/src/open-server.ts`, `scripts/ui`, graph dependencies                                                              | `bun run ui:e2e`, `bun run ui:e2e:graph`, `bunx temporal-explorer open --project fixtures/basic-order --trace success`, `bun run screenshots:verify`    | Graph, history, and trace views remain projections of validated artifacts.                   |
| MVP release verification         | main  | complete | Full checkout, package metadata, final scripts, implementation tracking                                                                           | Goal-objective final gate list                                                                                                                          | Final MVP gates passed after Stage 7.                                                        |

## MVP Workstream Closure

| Workstream                    | Starts After | Owner | Status   | Notes                                                                                                |
| ----------------------------- | ------------ | ----- | -------- | ---------------------------------------------------------------------------------------------------- |
| Schema and fixture truth      | Stage 0      | main  | complete | Artifact schemas and fixture validation are complete for the MVP.                                    |
| Static analyzer and CLI slice | Stage 1      | main  | complete | Static analysis flows through shared schemas, library APIs, and CLI output.                          |
| Runtime trace and mapper      | Stage 2      | main  | complete | History parsing is complete; Stage 4 owns source-aware mapping.                                      |
| Documentation and renderers   | Stage 4      | main  | complete | Deterministic Markdown and Mermaid outputs are generated from artifacts.                             |
| Explorer UI shell             | Stage 4      | main  | complete | SvelteKit shell loads committed artifacts and passes Playwright, accessibility, and lifecycle gates. |
| Graph and trace UI            | Stage 6      | main  | complete | Depends on local explorer shell and overlay artifact shape.                                          |
| CLI and library parity        | Stage 4      | main  | complete | MVP command parity is covered by public API tests and CLI fixture tests.                             |
| Release verification          | Stage 7      | main  | complete | Clean MVP verification passed and tracking files were audited.                                       |

## Post-MVP Construct Expansion (2026-07-01)

| Workstream                         | Owner    | Status      | Editable Scope                                                                             | Verification                                                   | Synchronization                                                                                                                   |
| ---------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------ | -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| Stage 9A signals/conditions/timers | main     | complete    | schemas, analyzer, history, mapper, renderers, cli, fixtures/approval, fixtures/timer-race | Stage 9 common gates                                           | Schema contracts landed before dependent layers.                                                                                  |
| Stage 9B-9H construct layers       | main     | complete    | schemas, analyzer, history, mapper, renderers, cli, nine construct fixtures                | Stage 9 common gates per slice via test:fixtures               | Implemented as layer passes; per-slice verification retained.                                                                     |
| Explorer UI construct support      | subagent | complete    | apps/explorer, scripts/ui                                                                  | bun test apps/explorer, ui:e2e, ui:e2e:graph, ui:accessibility | Consumed schema changes after each batch stabilized; found and fixed retried-vs-failed precedence and dropped dynamic confidence. |
| Stage 10 diagnostics + check       | main     | complete    | analyzer diagnostics modules, fixtures/diagnostics, cli check/doctor                       | check gates, CLI tests                                         | Severity overrides flow through temporal-explorer.config.ts.                                                                      |
| Stage 11 payload privacy           | main     | complete    | history payloads, fixtures/payloads, scripts/fixtures codec                                | payload fixture test, CLI warning test                         | Encrypted fixtures generated with a real reversible codec.                                                                        |
| Stage 12 schema governance         | main     | in_progress | packages/schemas json-schema, scripts/schema                                               | schema:emit-json-schema drift check, independent validator     | Version diagnostics and migration tests remain.                                                                                   |
| Stage 13 replay                    | main     | in_progress | scripts/replay                                                                             | replay:spike                                                   | Decision recorded; optional --replay mapping remains.                                                                             |
| Stage 18 library parity            | main     | in_progress | packages/api result/library-parity                                                         | api parity tests                                               | renderMarkdown byte-matches committed CLI docs; declarations and fetchEventHistory remain.                                        |
